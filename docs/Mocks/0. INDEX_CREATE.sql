CREATE INDEX funcion_fecha
  ON FUNCIONES (FECHA);

CREATE INDEX boleta_usuario
  ON BOLETAS (ID_USUARIO, ID_TIPO);

CREATE INDEX boleta_funcion
  ON BOLETAS (ID_LUGAR, FECHA);

CREATE UNIQUE INDEX localidad_nombre
  ON LOCALIDADES (NOMBRE);

CREATE UNIQUE INDEX requerimientos_nombre
  ON REQUERIMIENTOS_TECNICOS (NOMBRE);